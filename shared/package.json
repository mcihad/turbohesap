{
  "name": "@kentos/shared",
  "version": "0.1.0",
  "description": "Shared, framework-agnostic contracts for KentOS: DTO models, service interfaces, and their axios-based client implementations. Consumed by the web frontend and (later) the React Native mobile app.",
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "typescript": "~5.7.2"
  }
}
